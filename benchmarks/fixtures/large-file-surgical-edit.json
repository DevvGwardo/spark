{
  "id": "large-file-surgical-edit",
  "description": "Make a precise 2-line change in a large file without corrupting surrounding code",
  "prompt": "In server.ts, the rate limiter is set to 100 requests per minute. Change it to 50 requests per minute and change the window from 60000ms to 30000ms. Don't change anything else.",
  "repo": {
    "owner": "test-org",
    "name": "test-repo",
    "defaultBranch": "main",
    "fileTree": ["src/server.ts", "src/middleware.ts", "src/routes.ts"],
    "files": {
      "src/server.ts": "import express from 'express';\nimport cors from 'cors';\nimport helmet from 'helmet';\nimport morgan from 'morgan';\nimport { router } from './routes';\nimport { errorHandler } from './middleware';\n\nconst app = express();\n\n// Security middleware\napp.use(helmet());\napp.use(cors({ origin: process.env.CORS_ORIGIN || '*' }));\napp.use(express.json({ limit: '10mb' }));\napp.use(morgan('combined'));\n\n// Rate limiting configuration\nconst rateLimitConfig = {\n  windowMs: 60000,\n  maxRequests: 100,\n  message: 'Too many requests, please try again later.',\n  standardHeaders: true,\n  legacyHeaders: false,\n};\n\n// Apply rate limiting\nfunction rateLimiter(req: any, res: any, next: any) {\n  const clientIP = req.ip;\n  const now = Date.now();\n  const windowStart = now - rateLimitConfig.windowMs;\n\n  // Clean up old entries\n  const requests = requestLog.get(clientIP) || [];\n  const recentRequests = requests.filter((time: number) => time > windowStart);\n\n  if (recentRequests.length >= rateLimitConfig.maxRequests) {\n    res.status(429).json({ error: rateLimitConfig.message });\n    return;\n  }\n\n  recentRequests.push(now);\n  requestLog.set(clientIP, recentRequests);\n\n  if (rateLimitConfig.standardHeaders) {\n    res.setHeader('RateLimit-Limit', rateLimitConfig.maxRequests);\n    res.setHeader('RateLimit-Remaining', rateLimitConfig.maxRequests - recentRequests.length);\n    res.setHeader('RateLimit-Reset', Math.ceil((windowStart + rateLimitConfig.windowMs) / 1000));\n  }\n\n  next();\n}\n\nconst requestLog = new Map<string, number[]>();\n\napp.use(rateLimiter);\napp.use('/api', router);\napp.use(errorHandler);\n\n// Health check\napp.get('/health', (req, res) => {\n  res.json({\n    status: 'ok',\n    uptime: process.uptime(),\n    timestamp: new Date().toISOString(),\n    version: process.env.APP_VERSION || '1.0.0',\n  });\n});\n\n// Graceful shutdown\nconst server = app.listen(process.env.PORT || 3000, () => {\n  console.log(`Server running on port ${process.env.PORT || 3000}`);\n});\n\nprocess.on('SIGTERM', () => {\n  console.log('SIGTERM received, shutting down gracefully...');\n  server.close(() => {\n    console.log('Server closed');\n    process.exit(0);\n  });\n});\n\nexport default app;\n",
      "src/middleware.ts": "export function errorHandler(err: any, req: any, res: any, next: any) {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Internal server error' });\n}\n",
      "src/routes.ts": "import { Router } from 'express';\n\nexport const router = Router();\n\nrouter.get('/users', (req, res) => {\n  res.json([]);\n});\n"
    }
  },
  "expected": {
    "files_read": ["src/server.ts"],
    "changes": [
      {
        "path": "src/server.ts",
        "action": "edit",
        "mustContain": ["windowMs: 30000", "maxRequests: 50", "import express", "app.use(helmet())", "SIGTERM", "process.exit(0)"],
        "mustNotContain": ["windowMs: 60000", "maxRequests: 100"]
      }
    ],
    "unchanged": ["src/middleware.ts", "src/routes.ts"]
  }
}
